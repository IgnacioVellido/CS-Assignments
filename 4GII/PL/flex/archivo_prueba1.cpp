    // Cabecera programa
    // bloque
    // Inicio de bloque
    main {

    //declarar variables locales
        Variables { 
            int x,entero1, entero2 b,h 
            char _9s, fghh n; 
            bool variable1; 
            flo·at adioSS2;
            list of int listaenteros1; // no se puede poner _ entre medias en teoria
        }
€
        //Declar_de_subprogs
        float SubProg1(int param1, char v, bool _parAm3){ 
                // sentencia return
                return true;
        } 
€
        int hola (int hola, int adios) {
            Variables { 
                int x,entero1,entero2; 
                char _9s; 
                bool variable1; 
                float adioSS2;
                list of int listaenteros1; // no se puede poner _ entre medias en teoria
            }
€
            return hooola;            
        }    
€
        //Sentencias - prueba de todas

        // sentencia asignacion
        pepe128='o'; €

        // sentencia if
        if (a == b*){
            // sentencia entrada
            scanf("hola", pepe128, pepe129*-); //lo tenemos sin paréntesis
€
            while([22.3,false]){ //no tenemos comas aquí, lista explícita
            //sentencia avanza
            var1>>;
            //sentencia retroceder
            var2<<;
            // Suma
            x = x + 2;
            // Resta
            x = x - 2;
        }
€
        }
        else{ 
            print("prueba1", pepe128);
        }
€
        //sentencia switch
        switch(pepe128){ 
            case 'o':
                b=funcionPrueba(pepe128, true);
                //sentencia break 
                break; 
            case 'p':
                a=c ++ cc @ ccc;
            default:
                //sentencia comienzo
                a=!false;   
        }
€
        // errores de léxico para testing
€
        // testing errores sintáctico
        a = pepe122; // falta pyc
        x = -2;
        x = x+1;
        x = +1;
€
        x = 2 >< 3;
    // fin de bloque  
€    }